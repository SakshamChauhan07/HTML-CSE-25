<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Unnati!</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;800&family=Dancing+Script:wght@700&display=swap" rel="stylesheet">
    <style>
        /* --- CSS Styles --- */
        :root {
            --color-bg-1: #ff00cc;
            --color-bg-2: #3333ff;
            --color-bg-3: #00ffcc;
            --color-bg-4: #ffff00;
            --color-bg-5: #ff3300;
        }

        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden; /* Prevents scrollbars during animations */
            font-family: 'Poppins', sans-serif;
        }

        /* The moving colorful background */
        .animated-background {
            position: absolute;
            top: 0;
            left: 0;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, var(--color-bg-1), var(--color-bg-2), var(--color-bg-3), var(--color-bg-4), var(--color-bg-5), var(--color-bg-1));
            background-size: 400% 400%;
            z-index: -1;
            animation: flowingGradient 15s ease infinite;
        }

        @keyframes flowingGradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* The main card container - using "Glassmorphism" effect */
        .card-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            
            background: rgba(255, 255, 255, 0.25); /* Semi-transparent white */
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            backdrop-filter: blur(10px); /* The frosted glass effect */
            -webkit-backdrop-filter: blur(10px);
            border-radius: 25px;
            border: 1px solid rgba(255, 255, 255, 0.18);
            
            padding: 50px;
            text-align: center;
            max-width: 80%;
            width: 500px;
            color: white;
        }

        /* Typography & Text Animations */
        h1 {
            font-family: 'Dancing Script', cursive; /* Fancy script font */
            font-size: 4rem;
            margin: 0;
            text-shadow: 3px 3px 6px rgba(0,0,0,0.3);
            
            /* Entrance Animation */
            opacity: 0;
            animation: slideUp 1s ease-out forwards;
            animation-delay: 0.5s;
        }

        h2 {
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-top: 5px;
            margin-bottom: 0;
            font-size: 2.5rem;